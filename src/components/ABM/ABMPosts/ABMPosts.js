import React from 'react';
import {Form, FormGroup, FormControl, Button} from "react-bootstrap";

export default function ABMPosts() {
    return (
        <Form>
            <FormGroup>
                <FormControl/>
            </FormGroup>
            <FormGroup>
                <FormControl/>
            </FormGroup>
            <FormGroup>
                <Button>Guardar</Button>
                <Button>Cancelar</Button>
            </FormGroup>

        </Form>
    )
}
