import React from 'react';
import {Button, Form, FormGroup, FormControl} from "react-bootstrap";

export default function ABMAlbums() {
    return (
      <Form>
          <FormGroup>
            <FormControl/>           
        </FormGroup>
       <FormGroup>
           <Button>Guardar</Button>
           <Button>Cancelar</Button>
       </FormGroup>
      </Form>
    )
}
